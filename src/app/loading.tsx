import React from "react";

const Loading: React.FC = () => {
  return (
    <div className="flex min-h-screen flex-col items-center  p-24 bg-gray-300">
      Loading Page
    </div>
  );
};

export default Loading;
